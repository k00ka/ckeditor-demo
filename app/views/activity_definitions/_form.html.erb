<%= form_for(@activity_definition) do |f| %>
  <% if @activity_definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_definition.errors.count, "error") %> prohibited this activity_definition from being saved:</h2>

      <ul>
      <% @activity_definition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :html_blob %><br />
    <%= f.cktext_area :html_blob, ckeditor: { language: 'en'} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
